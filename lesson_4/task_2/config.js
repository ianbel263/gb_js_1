'use strict';

// Такое именование используется для перечисления константных значений (аналог enum в TypeScript)
const MapSize = {
    COLS: 10,
    ROWS: 10
};

const BeginCoords = {
    COL: 0,
    ROW: 0
}
